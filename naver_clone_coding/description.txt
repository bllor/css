프로그래머는 생각을 하는 사람
만들 전에 어떻게 만들지 먼저 생각을 해야 한다.
가로,세로법칙
1.가로로 먼저 자르고, 자른 가로를 세로로 자른다.
프로그래밍에는 정답이 없다, 효율적인 답이 있을 뿐
여백은 굳이 자르지 않고 위나 아래에 붙여도 되지만,
반응형이 어떻게 변하느냐 따라 위, 아래에 붙이면 된다.
일하는 시간 중 생각하는 시간보다 더 타자를 치는 시간이 많다면
금방 대체될 가능성이 크다.

head부분은 메타데이터, body는 데이터

display : inline-block -- 주어진 너비만큼만 공간을 특성을 차지
display : inline -- 높이와 넓이를 무시한다.

position: absolute - 부모 또는 조상 중에 포지션 스태틱이 아닌 것이 기준이 된다.
position: fixed - 스크롤을 내려도 현위치에 고정
position: sticky - absolute처럼 있다가 스크롤을 내리면 fixed가 됨.

모든 태그에는 before, after 가상태그를 사용할 수 있다.
가상태그는 자식요소라고 볼 수 있다.

z-index는 형제 관계일 때만 사용가능 하다.

display:none 이것을 많이 사용하지 않는 것이 좋은데, 시각장애인 같은 분이 웹사이트에 들어갔을 때 컨탠츠가 읽히지 않는다.

box-sizing: border-box; 이렇게 변경할 경우, 패딩과 높이를 같이 계산해준다.

css는 아래에 선언된 것이 위에 선언된 것을 오버라이드 한다.

아이디는 하나만 만들 수 있고, 클래스는 여러 개 만들 수 있다.
어떤 형식으로 만들어야할 지 모르겠으면, 일단 아이디로 만든 다음, 다른 곳에서 사용되면 클래스로 바꾼다.

스타일 태그를 바디에 쓰면 안되는 이유?
1. 코드의 재사용성이 어려워진다.
2. 레이아웃 변화가 발생할 수 있다
--> body 태그 안에 있는 스타일 태그는 문서가 로딩된 후에 적용되므로, 화면의 깜박임이나 레이아웃 변화가 발생할 수 있다.
3.html 표준을 위배한다. style은 head 안에 존재해야함.

svg vector img: 아무리 확대를 해도 깨지지 않음
